# 保险商机智能整理与推送助手 v2

`opportunity_app.py` 是与 `app.py` 标书核对系统并列、但代码和页面完全隔离的第二个 Streamlit 应用。它将“获取今日商机、险种/工程性质判断、金额门槛、区域分配、内部确认、群文案、成都长图和 Excel”整合成一条可追溯流程。

v2 的主要变化：

- 从只能上传乙方宝 Excel，升级为**会员 Excel＋四川官方公开来源**双模式；
- 确定性规则仍先筛，`deepseek-v4-flash` 成为所有候选商机的必经语义审查层；
- 官方正文只向模型发送有界、相关、已脱敏的证据片段；
- 正式群文案、PNG 和 Excel 增加质量闸门，不确定项不能静默混入推送包；
- 既可在部门老师的 Windows 电脑本地运行，也可从同一 GitHub 仓库部署为第二个 Streamlit Community Cloud URL。

## 1. 数据来源双模式

### 模式 A：会员 Excel 导入

1. 在乙方宝分别搜索“险”和“工程”。
2. 使用公司合法获得的会员功能导出原始 `.xls/.xlsx`。
3. 不要手工修改扩展名，分别上传两份文件；如果放反，系统会根据内容识别并交换。
4. 点击“开始智能整理”，系统依次执行解析、规则预筛、AI 审查和区域分配。

该模式只处理公司已获得授权并合法导出的文件，不扩大会员权限。

### 模式 B：四川官方公开来源（免费）

1. 选择“官方公开来源（免费）”。
2. 设置开始日期、结束日期和每个关键词的最多采集数。
3. 系统先在四川省公共资源交易官方公开信息中按标题召回“险”和“工程”，过滤结果类公告，再只为规则候选读取官网完整正文、金额证据和官方链接。
4. 页面保留官方来源、证据摘录和原文链接，便于经办人回查。

#### 免费模式的明确边界

- 只访问无需登录的政府/公共资源交易官方公开信息。
- 不登录乙方宝，不复制 Cookie、Token 或签名参数，不规避验证码、频控或付费墙。
- 官方数据源的覆盖范围、更新时间、字段完整性和网络可用性可能变化。
- **不保证覆盖乙方宝汇聚的全部商业来源**，不应在未做对账验收前宣称可 100% 替代会员服务。

建议使用同一日期范围同时运行两种模式，持续记录原始数、有效商机数、正文获取率、金额完整率、重复率和官方链接命中率，累积足够样本后再评估会员依赖程度。

## 2. 统一处理链

```text
会员 Excel / 官方公开来源
        ↓
标题召回、结果类过滤、格式标准化、地区归一
        ↓
Python 确定性规则预筛
        ↓
仅为候选读取官网完整正文并抽取金额证据
        ↓
DeepSeek V4 Flash 批量审查候选
        ↓
高置信自动纳入/筛除，其余进入内部确认
        ↓
区域分配＋业务人员确认
        ↓
质量闸门
        ↓
群文案＋成都 PNG＋专业 Excel
```

### 确定性规则先筛

规则引擎不让模型承担可以精确计算的任务：

- 清理空值、占位符、Excel 日期和“元/万/亿”金额单位；
- 标记不合理的超大金额、缺失金额和低于工程门槛的项目；
- 识别删除公告、明显非工程语义、保险子串误命中和补充/更正版本；
- 生成稳定项目键和公告键；跨“险/工程”命中的同一官方公告只保留一条正式推送记录；
- 按市、区县和无服务点规则进行可重现的区域分配。

### 固化业务口径

- 保险仅纳入：工程险、货运险、意外险、健康险、责任险、企财险、信用险、保证险；不设金额门槛。
- 工程默认金额门槛为 `10,000,000` 元，可在页面调整；官方正文优先用项目/标段投资额衡量工程规模，未披露时再回退到控制价、最高限价、预算或合同估算价，并保留金额口径与原文依据。
- 成都的成华区、锦江区、高新区、天府新区默认为“无区域类”，名单可在侧边栏调整。
- 成都其他区县按区县分配；四川省内非成都项目归“川内其他地区”。
- 区县未披露和成都无服务点项目不伪造归属，正式报告中展示为“未分区域项目”。

## 3. DeepSeek V4 Flash 必经审查

搜索“险”会命中“保险孔螺钉、医疗保险系统、风险评估、排危险”等子串噪声；搜索“工程”也会命中代理机构名称、资质表述或非施工采购。规则可以高效排除明显项，模糊语义则由模型批量二次审查。

本版不提供“跳过 AI 生成正式包”的通道：

- 未配置 `DEEPSEEK_API_KEY` 时，处理按钮不可用；
- 默认模型为 `deepseek-v4-flash`；
- 保险候选全部进行语义二次审查；
- 工程仅将未低于金额门槛、且未被规则明确排除的候选送审；
- 高置信的 `include/exclude` 只有在理由能与标题/正文形成证据锚点时才自动应用；否则默认不勾选并转内部确认；
- 单次 AI 候选有安全上限和全局时限，密钥、余额或网络故障会熔断剩余批次；若整次 AI 无有效结果，全部候选保持内部确认，正式输出仍被锁定，不会伪造“已审查”状态。

### 送模数据最小化

模型请求只包含记录 ID、项目标题、公告/项目阶段、标准化金额、地区、来源类型（保险/工程），以及如有官方正文时的局部证据片段。

正文不会整篇上送。证据片段仅围绕险种、工程性质、预算/投资额等标记截取，长度有上限，并再次移除联系人姓名、手机/座机、邮箱和 URL；这些联系字段也不会作为独立字段进入模型请求。

注意：脱敏不等于数据不离开公司环境。项目标题、地区、金额和证据片段仍会发送到配置的 DeepSeek API，使用真实业务数据前必须符合公司的第三方模型调用规范。

## 4. 质量闸门

点击生成正式推送包时，系统会重新检查所有已勾选记录。存在任一下列情形，群文案、PNG 和 Excel 会被阻止：

- 记录仍标记为“需内部确认”；
- 金额状态为明显异常；
- 工程金额缺失、低于门槛或不是正常可核验金额；
- 保险分类仍是候选、未确定、无关或待判断；
- 工程分类仍是需确认或非工程；
- 没有可验证的 AI `include` 结果，且业务人员也未完成合法分类和人工确认。
- 官方公开记录未成功取得官网完整正文，且业务人员未人工确认。

区县未披露、成都无服务点本身不代表业务性质不合格。只要险种/工程性质、金额和 AI/人工确认已通过，这类记录仍可进入“未分区域项目”。

## 5. 每日操作

1. 选择“会员 Excel 导入”或“官方公开来源（免费）”。
2. 上传两份原始表，或设置公开来源日期范围与最大采集数。
3. 启动处理并等待规则＋DeepSeek 审查完成。
4. 检查顶部统计、数据质量提示和“AI 审查记录”。
5. 在保险/工程表中确认“推送”、险种/工程类别、区域归属、确认意见和推送备注。
6. 内部确认项如要纳入，必须先选择明确的八险种或“直接施工/前期线索”，再勾选推送；不能确定则保持不勾选。
7. 生成正式推送包。如被质量闸门拦截，按页面列出的项目完成确认后再生成。
8. 复制群文案，下载 PNG 和 Excel 发送至企业微信群。

## 6. 输出内容

- **简洁群文案**：适合日常推送，避免几十个长标题刷屏。
- **完整群文案**：显示项目、分类、金额、截止时间、招标人/采购人、联系方式和官方链接。
- **成都地区 PNG**：按保险/工程和服务区域排列成都已纳入项目。
- **专业 Excel**：固定 9 页，包括今日汇总、成都/川内保险与工程、未分区域项目、筛除记录、采集与判定审计和处理日志。四个正式业务明细页只保留 15 个必要字段；审计页单独保留官方来源、金额口径与证据、正文取证状态、AI 判断及官方链接。

所有下载物均通过 `BytesIO` 在内存中生成，只有用户点击下载后才由浏览器保存。

## 7. 本地运行

### Windows 一键启动

推荐 Python 3.12，然后双击 `启动商机推送助手.bat`。首次运行会创建 `.venv` 并安装依赖，默认访问：

```text
http://localhost:8502
```

### PowerShell 手动启动

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python -m streamlit run opportunity_app.py --server.address localhost --server.port 8502
```

本地 `.streamlit/secrets.toml`：

```toml
DEEPSEEK_API_KEY = "你的 DeepSeek API Key"
DEEPSEEK_BASE_URL = "https://api.deepseek.com"
DEEPSEEK_MODEL = "deepseek-v4-flash"
```

`.streamlit/secrets.toml` 必须保持在 `.gitignore` 中，不得提交。

## 8. 同仓库部署第二个 Streamlit Cloud URL

Streamlit Community Cloud 官方支持同一仓库部署多个应用。两个入口共用根目录的 `requirements.txt` 和 `.streamlit/config.toml`，但每个部署有独立 URL 和 App settings。

1. 将当前仓库推送到 GitHub。
2. 登录 [Streamlit Community Cloud](https://share.streamlit.io/)，点击 **Create app**。
3. 选择与标书系统相同的 repository 和 branch。
4. 将入口文件设为 `opportunity_app.py`，可选设置独立子域名。
5. 在新 App 的 **App settings → Secrets** 单独填入：

   ```toml
   DEEPSEEK_API_KEY = "你的 DeepSeek API Key"
   DEEPSEEK_BASE_URL = "https://api.deepseek.com"
   DEEPSEEK_MODEL = "deepseek-v4-flash"
   ```

6. 部署并 Reboot；侧边栏应显示模型已连接，且页面同时显示两种来源模式。

官方资料：

- [同仓库多应用与文件结构](https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/file-organization)
- [Community Cloud 部署](https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/deploy)
- [Secrets management](https://docs.streamlit.io/deploy/streamlit-community-cloud/deploy-your-app/secrets-management)
- [公开/私有访问与邀请](https://docs.streamlit.io/deploy/streamlit-community-cloud/share-your-app)

不要假设第一个 App 的 Secrets 会自动继承给第二个 App。Community Cloud 当前的私有应用数量和观看者权限有平台限制，部署前应按官方分享文档核对当前账户能力。

## 9. 数据安全与云端隐私

### 本地运行

当 `--server.address localhost` 时，上传表格、中间数据和报告在部门老师的电脑与当前 Streamlit 会话内处理。程序不会把报告写到固定绝对路径，但 DeepSeek 送审的脱敏最小字段仍会经网络发给模型 API。

### Community Cloud

在 Community Cloud 上，`st.file_uploader` 的文件会从用户浏览器发送到 Streamlit 云端后端。Streamlit 官方说明上传文件默认保存在 Python RAM，当替换/清空文件或关闭上传页签后删除；但这不等同于数据从未离开个人电脑。参见 [Streamlit 上传文件存储说明](https://docs.streamlit.io/knowledge-base/using-streamlit/where-file-uploader-store-when-deleted)。

Streamlit 官方还说明 Community Cloud 当前主机均在美国且不可配置。参见 [Community Cloud status and limitations](https://docs.streamlit.io/deploy/streamlit-community-cloud/status)。

因此，对真实商机、联系人、手机、座机、邮箱或其他企业数据：

- 未获得公司信息安全、数据合规和第三方模型使用批准前，**建议仅在老师的本地电脑使用真实数据**；
- Community Cloud 只使用脱敏、伪造或经批准的测试表演示；
- 如经公司批准使用云端，建议将 App 设为私有，仅按邮箱邀请必要人员，并定期清理观看者；
- 不应在缓存、处理日志或错误信息中长期保留原始联系人数据；
- 私有 URL 和 HTTPS 不能替代公司内部审批。

## 10. 当前真实数据回归基准

使用 2026-08-26 提供的乙方宝原始源表：

- “险”源表共 63 条，确定性规则的旧基准为纳入 10 条公告、对应 9 个独立商机，金额合计 `2,207,033` 元；
- “成都邛崃智选假日酒店雇员忠诚险和现金险”应保持内部确认，不应由规则静默纳入；
- “工程”源表共 55 条，应识别 `4.50701E+17` 的异常金额和“已更新即将删除”的旧公告。

这些数字用于锁定 Excel 解析、金额清洗和确定性规则的回归口径。v2 正式结果还必须经过当前 `deepseek-v4-flash` 审查，因此不应把旧规则数字误解为每次 AI 审查后都必须原样不变的业务结果。

## 11. 测试

```powershell
python -m pytest -q
python -m compileall -q app.py opportunity_app.py opportunity_assistant
```

离线测试使用伪造数据和模拟模型响应，不会调用真实 DeepSeek API。正式上线前还应分别对会员 Excel 和官方公开来源执行端到端验收，记录运行时间、模型批次、有效返回率、质量闸门拦截数和人工抽检结果。

## 12. 功能边界

- 本工具是商机线索整理与推送助手，不代替对投标资质、承保能力、业务合规、招标文件和截止时间的最终人工确认。
- AI 可能误判；质量闸门可以防止已知不确定项进入正式包，但不代表输出零风险。
- 官方网站结构、字段和可用性变化可能导致部分项目延迟或无法获取正文；系统应显示质量状态，不猜测缺失信息。
- 免费模式只是官方公开数据处理通道，不是对任何商业平台权限的规避。
